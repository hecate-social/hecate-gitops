# Headless service for Erlang clustering discovery
apiVersion: v1
kind: Service
metadata:
  name: hecate-daemon-headless
  namespace: hecate
spec:
  clusterIP: None
  selector:
    app: hecate-daemon
  ports:
    - name: epmd
      port: 4369
    - name: erlang-dist
      port: 9100
